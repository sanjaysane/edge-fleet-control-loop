version: "3.8"
services:
  control:
    build: ./control_plane
    ports:
      - "8000:8000"
    volumes:
      - ./control_plane:/app
      - ./dashboard:/app/dashboard_static
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --reload
